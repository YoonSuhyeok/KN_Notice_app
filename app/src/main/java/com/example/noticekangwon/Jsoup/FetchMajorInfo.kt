package com.example.noticekangwon.Jsoup

class FetchMajorInfo(index: Int, fk: Int, isSelect: Boolean, baseUrl: String, cutBaseUrlNumber: Int, cutpatchUrlNumber: Int) {
    val index = index
    val fk = fk
    var isSelect:Boolean = isSelect
    val baseUrl = baseUrl
    val cutBaseUrlNumber = cutBaseUrlNumber
    val cutpatchUrlNumber = cutpatchUrlNumber
}